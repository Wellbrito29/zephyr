/**
 * @format
 *
 * The dynamic import creates the async boundary Module Federation needs to
 * initialize shared dependencies (react / react-native) before app code runs.
 */
import('./bootstrap');
